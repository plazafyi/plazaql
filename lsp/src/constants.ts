/** Debounce delay for publishing diagnostics after document changes (ms). */
export const DIAGNOSTIC_DEBOUNCE_MS = 150

/** Maximum line length before the formatter wraps to multi-line. */
export const MAX_LINE_LENGTH = 80
